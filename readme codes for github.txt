git status

git add .

git commit
: wq [esc key]

git push


Navigation links are added/edited in _data/navigation.yml by uncommenting or adding items under main
Page content is edited in _pages/research.md (or similar), below the --- front matter block
Dropdown menus use a children key under a nav item in navigation.yml
Site title (top-left home link) is set via title: in _config.yml — yours was set to ""
Resources page — keep it simple for now with a single link to your datasets PDF, and expand to a dropdown later as you add more content
